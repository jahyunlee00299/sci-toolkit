# Third-party packages each skill's scripts import at module level.
# GENERATED by scripts/skill_requirements.py scan — do not edit by hand;
# tests/test_skill_requirements.py fails when this file lags the scripts.
# Skills without scripts/ have no entry. Windows-only packages: pywin32.

[biorxiv-database]
packages = []

[conda-env-manager]
packages = ["pyyaml"]
# pyyaml: import yaml

[generate-image]
packages = []

[get-available-resources]
packages = []

[journal-presentation-maker]
packages = []

[literature-review]
packages = ["requests"]

[manuscript-pipeline]
packages = ["defusedxml", "python-docx", "pywin32"]
# python-docx: import docx
# pywin32: import win32com

[markitdown]
packages = ["markitdown", "openai"]

[openalex-database]
packages = ["requests"]

[paper-extract]
packages = []

[patent-invention-disclosure]
packages = ["python-docx"]
# python-docx: import docx

[publication-figures]
packages = ["cycler", "matplotlib", "numpy", "pillow", "scikit-image"]
# pillow: import PIL
# scikit-image: import skimage

[research-lookup]
packages = []

[scientific-validation]
packages = []

[stats-workflow]
packages = []

[web-scraping]
packages = ["beautifulsoup4"]
# beautifulsoup4: import bs4
