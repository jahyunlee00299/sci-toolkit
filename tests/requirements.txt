# Third-party packages needed to run the 12 regression tests — this is all of them.
#
# This list was obtained by **actually running in a clean venv**, not by
# grepping (260807). A top-level-import-only scan gets it wrong:
# assumption_check.py lazy-imports scipy inside a function, and pandas inside
# load_table(). It took two CI round-trips to find that all three are needed.
#
# The other 11 tests use only the standard library. Packages the skills use
# overall (matplotlib, python-docx, rdkit, Pillow, openpyxl, ...) are not
# listed here — the tests never exercise that code path, so adding them would
# only slow down CI install time.
#
# To verify:
#   python -m venv /tmp/ci && /tmp/ci/bin/pip install -r tests/requirements.txt
#   for t in tests/test_*.py; do /tmp/ci/bin/python "$t" || echo "FAIL $t"; done

numpy
scipy
pandas
# The web-scraping pytest suite (registered in doctor 2026-09-02) exercises
# fetch_academic's CrossRef CLI path, which exits before the mock is reached
# when habanero is absent. Measured 2026-09-03 on the Linux home PC: 3 of 129
# cases failed for that reason while the laptop (habanero installed) passed.
# The three cases now importorskip; installing it here keeps them RUNNING in
# CI instead of skipped.
habanero
# tests/test_skill_contract.py parses frontmatter with PyYAML when present
# (falls back to a key scan without it).
pyyaml
# pytest-style checks (tests/test_sci_http.py, skills/web-scraping/tests, and
# the pytest cases inside test_doctor_selftest_verdicts) run under
# `python -m pytest`. Measured 2026-09-03: the first CI run after they were
# registered had no pytest and every one of them exited 1 with no FAIL line.
pytest
# tests/test_tool_cli_smoke.py runs `fetch_github.py --help`, which imports
# httpx at module level (measured on the same CI run).
httpx
